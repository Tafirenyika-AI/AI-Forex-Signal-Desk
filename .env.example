OANDA_API_TOKEN=your_oanda_api_token_here
OANDA_ENVIRONMENT=practice
OANDA_ACCOUNT_ID=
FRED_API_KEY=
ALPHAVANTAGE_API_KEY=
ALPACA_API_KEY=
ALPACA_API_SECRET=
ALPACA_BASE_URL=https://paper-api.alpaca.markets/v2

# Optional. Leave unset to use a local SQLite file (data/forex.db), which is
# the default and requires nothing else below. Set this (a postgresql://
# connection string) to instead use a shared, always-on hosted Postgres
# database — the same one both this local instance and any cloud-hosted
# dashboard connect to, for real local/cloud parity. See src/data/db.py's
# get_engine() docstring for why Postgres over SQLite-compatible options.
DATABASE_URL=
